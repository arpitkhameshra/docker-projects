name: Hello World

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Welcome to GitHub Action
        run: echo "Welcome to GitHub Action"
        shell: bash 
  
    runs-on: ubuntu-latest
    steps:
    - name: Goodbye
      run: echo "Goodbye to GitHub Action"
      shell: bash 
      
    
